import React from "react";
import { Paragraph } from "./Style";

const Main = ({ minhasInfos }) => {
  return (
    <div>
      <Paragraph>{minhasInfos}</Paragraph>
    </div>
  );
};

export default Main;
